services:
  backend:
    build: .
    container_name: coffeemind-backend
    ports:
      - "8000:8000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: coffeemind-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped
